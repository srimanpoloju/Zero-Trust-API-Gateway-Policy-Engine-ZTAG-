# Environment variables for ZTAG platform
# These values are used by Docker Compose and individual services during local development.

# --- General Configuration ---
LOG_LEVEL=debug # info, debug, warn, error
JWT_SECRET="super-secret-jwt-key-change-in-production,another-jwt-secret-for-rotation"

# --- Database (PostgreSQL) ---
DATABASE_URL="postgresql://ztag_user:ztag_password@postgres:5432/ztag_db"
POSTGRES_USER=ztag_user
POSTGRES_PASSWORD=ztag_password
POSTGRES_DB=ztag_db

# --- Redis ---
REDIS_URL="redis://redis:6379"

# --- ZTAG Gateway Service ---
GATEWAY_PORT=3001
# URLs for downstream services the Gateway proxies to. Use service names for Docker Compose, localhost for pnpm dev.
POLICY_ENGINE_URL="http://policy-engine:4000" # For Docker: http://policy-engine:4000, For pnpm dev: http://localhost:4000
ECHO_SERVICE_URL="http://echo-service:7070"   # For Docker: http://echo-service:7070, For pnpm dev: http://localhost:7070

# --- ZTAG Policy Engine Service ---
POLICY_ENGINE_PORT=4000

# --- ZTAG Control Plane UI ---
CONTROL_PLANE_PORT=3000
# Publicly accessible URL for the Policy Engine, used by the Control Plane's frontend code.
NEXT_PUBLIC_POLICY_ENGINE_URL="http://localhost:4000"

# --- ZTAG Echo Service ---
ECHO_SERVICE_PORT=7070

# --- Node.js Environment ---
NODE_ENV=development # development, production
